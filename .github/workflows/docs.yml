name: Documentation

on:
  push:
    branches:
      - main
    paths:
      - "docs/**"
      - ".gitmodules"
      - ".shared/productive-k3s-docs-theme"
      - ".github/workflows/docs.yml"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: docs-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-24.04
    env:
      NEWSLETTER_TURNSTILE_SITE_KEY: ${{ vars.NEWSLETTER_TURNSTILE_SITE_KEY }}

    steps:
      - name: Check out repository
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"
          cache-dependency-path: docs/requirements.txt

      - name: Build docs
        shell: bash
        run: |
          set -euo pipefail
          chmod +x docs/build.sh
          ./docs/build.sh

      - name: Deploy to gh-pages
        uses: peaceiris/actions-gh-pages@329bcc8f12caed2cefe5a5b80781499a6f3b361b # peaceiris/actions-gh-pages@v4
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./docs/site
          publish_branch: gh-pages
          cname: profiles.productive-k3s.io
          force_orphan: true
          commit_message: "docs: publish site [skip ci]"
